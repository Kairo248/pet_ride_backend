# Nixpacks configuration for Railway
# This ensures Maven builds the project before running

[phases.setup]
nixPkgs = ["maven", "jdk21"]

[phases.build]
cmds = [
  "chmod +x build.sh start.sh || true",
  "./build.sh"
]

[phases.start]
cmd = "./start.sh"

[staticAssets]
path = "target"

